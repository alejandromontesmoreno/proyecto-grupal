Utilizando la clase Operaciones, realizar la operación asignada.
De igual manera deberás realizar la prueba correspondiente en el archivo main.py

Nombre              |   Operación
-----------------------------------------------------------------------------
Ana Paula           | maximo(lista) -> Regresa el número mayor de la lista
Francisco Eduardo   | minimo(lista) -> Regresa el número menor de la lista
Diego Arturo        | promedio(lista) -> Regresa el promedio de los números de la lista
Alexander           | ordenarLista(lista) -> Regresa la lista ordenada
Diego Horner        | eliminarDuplicados(lista) -> Regresa la lista sin números duplicados
Mia Ana Itzel       | esPar(numero) -> Regresar True si el número es par False en caso contrario
Mauricio            | esImpar(numero) -> Regresa True si el número es impar False en caso contrario
Luis Antonio        | numeroMayo(a,b,c) -> Regresa el número mayor de los tres números recibidos 
Sebastián           | generarTablaMultiplicar(numero) -> Imprime la tabla de multiplicar del número indicado
Diego de Jesús      | contarPalabraEnTexto(texto,palabra) -> Regresa el número de veces que se encuentra la palabra en el texto
Julian              | serieFibonacci(n) -> Imprime la serie fibonacci hasta el número n
Paulina             | reemplazarEspacios(texto, caracter) -> Regresa una cadena con el texto reemplando los espacios con el caracter